// This is a placeholder file for compatibility with code that was migrated from Create React App
// In Vite, we don't need this functionality but keeping a stub for compatibility

const reportWebVitals = (onPerfEntry?: any) => {
  // Not implemented in Vite version
  if (onPerfEntry && typeof onPerfEntry === 'function') {
    // No-op
  }
};

export default reportWebVitals;